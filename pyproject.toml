[project]
name = "employee-analytics-tool"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "pandas",
    "numpy"
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = ["test"]
